tools
	new post
	render page from markdown body
	fixup typography (orphans, quote directions)

navigation
	sidebar on large screens

pages
	links
	per-conlang

tags
	user tags
	auto tags (search content for keywords)
	lists of posts by tag

RENAME
	${config} --> ${meta}
	.in --> .html.frag
	setconfig --> pyon
	mako <template> <content> <pyon>